import React from 'react';

const Success = () => {
    return (
        <form className="form form--primary">
            <div className="form__control full">{shippingMethodList}</div>
        </form>
    );
};

export default Success;